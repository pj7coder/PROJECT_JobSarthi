/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/JSnext',
  // Disable react strict mode during migration if there are side effects, but not required
  reactStrictMode: false,
};

export default nextConfig;
